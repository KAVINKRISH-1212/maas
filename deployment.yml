apiVersion: apps/v1
kind: Deployment
metadata:
  name: "mydeploy"
spec:
  replicas: 3
  template:
    metadata:
      name: "mypod"
    spec:
      containers:
        - name: "myc1"
          image: "kavinkrish/maas:v1"    
